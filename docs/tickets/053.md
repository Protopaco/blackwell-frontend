# [053] devTestData scenario revamp

**Board status:** Backlog
**Part of:** [048] Expand EmployeeActivityRates (Epic)
**Depends on:** [049], [051], [052]

## Background

**This is `blackwell-backend` work.**

`src/devTestData/scenarios/createConfiguredClientSetup.ts` seeds 3 activities with the old `payRate`/`flatRateAmount` and 3 employees with the old `hourlyPayRate1/2`/`holidayPayRate` — these fields won't exist anymore after [051]/[052]. Dev/test data needs to seed bridge rows instead, or nothing downstream can run against realistic data.

## Scope

- Update `createConfiguredClientSetup.ts` (and any other scenario builders referencing the removed fields) to seed `EmployeeActivityRates` bridge rows for its employees/activities, covering at least one of each `payRateType` (hourly, flatrate, salary) so downstream tickets have data to exercise
- Update `Employee`/`Activity` seed data to match the new shape (`salaryAmount` on at least one seeded employee)
- Update any other devTestData builders/scenarios that reference the removed fields
