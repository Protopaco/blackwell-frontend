# [014] Create Frontend Tests

**Board status:** Backlog
**Migrated from:** old `docs/TICKETS.md` "Write component/unit tests" note (deferred 2026-07-09)

## Background

Testing philosophy and the tested-vs-not criteria are already settled and documented in `STYLE_GUIDE.md` §11 (test what has branching/computation/is shared/is async; skip pure passthrough). Writing tests was deliberately deferred rather than skipped — originally because Client Summary's cards were still actively changing shape early on; that reasoning is stale now given how much has been built and stabilized since (Employee/Holiday/Funding Source/Activity Management, the full Pay Period pipeline).

## Scope

Not yet scoped for what exists today. Likely candidates per the existing criteria in `STYLE_GUIDE.md` §11 — needs a fresh pass to identify what actually has branching/shared logic worth testing across the now much larger built surface (management pages' hooks/forms, `PayrollReportPage`/`AllocationReportPage`'s save+regenerate logic, shared hooks like `useFetchByKey`/`useAsyncAction`).

## Open questions

- Full sweep across everything built so far, or prioritize the highest-risk/most-complex logic first (e.g. the auto-regenerate-on-save mechanism, batch upsert logic)?
