# [040] Restyle Client Selector

**Board status:** Backlog

## Background

`ClientSelector.tsx` (`src/components/Header/ClientSelector/ClientSelector.tsx`) is a MUI `Autocomplete` in the app bar, currently styled only via `min-width: 240px` in `ClientSelector.css` — no other custom styling applied.

## Scope

TBD — specifics of the desired restyle (sizing, colors, label treatment, etc.) to be discussed before implementation.
