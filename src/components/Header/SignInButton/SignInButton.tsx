import Button from '@mui/material/Button';
import '@/components/Header/SignInButton/SignInButton.css';

const SignInButton = () => {
  return (
    <Button color="inherit" id="sign-in-button">
      Sign In
    </Button>
  );
};

export default SignInButton;
