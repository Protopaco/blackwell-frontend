# [044] BUG - Holiday styling looks off (coloration)

**Board status:** Backlog
**Reported:** 2026-07-27

## Background

**This is `blackwell-frontend` work.**

Holiday coloration looks weird somewhere in the app. Reported as coloration only — no other layout/content issues. Which surface (Pay Period dashboard `HolidaysCard`, Client Summary `ClientHolidaysCard`, or the `HolidaysManagement` table) and exactly what looks wrong is not yet specified — needs a screenshot or more detail from whoever picks this up.

Note: as of this writing, none of the three holiday surfaces apply any holiday-specific custom coloring — they all inherit plain MUI theme defaults via the shared `DashboardList`/`DashboardCard` components (`HolidaysManagement`'s table also uses default MUI colors throughout). So the "weird" look is likely a default-theme/contrast issue rather than a bug in bespoke holiday styling — worth confirming which surface and what looks wrong before touching anything.

## Scope

TBD — pending clarification on which surface and what specifically looks wrong.
