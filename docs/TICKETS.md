# BlackwellTime Frontend — Ticket Board

Working ticket list for building the frontend described in the backend's `docs/UI.md`, cross-referenced against `docs/openapi.json`. Structured like a kanban board: grouped by phase (the real dependency structure), each ticket carries a `Status` you flip as work happens.

**Status values**: `Backlog` (upstream dependency not done) → `Ready` (dependencies met, safe to start a fresh session on it) → `In Progress` → `Done`.

Intended workflow: one ticket per conversation/session, to keep each session focused and avoid one giant thread. Update this file's Status column as tickets move.

**Before writing any code on a ticket**: discuss and resolve its open UI/UX/implementation questions first (e.g. is the client selector a dropdown or a searchable field, exact card layout, etc.) and get explicit go-ahead. Do not start implementing on assumptions — surface the question, wait for an answer, then build.

**Never mark a ticket `Done` without explicit approval.** When implementation finishes, report what was built and leave the Status as `In Progress` (or similar) until the user reviews and says to mark it `Done` — don't self-approve.

**Reference project**: `/Users/paulstevens/Developer/Babeonym/babeonym-frontend` is a sibling app built from the same base template, more fleshed out. Its organizational conventions have been codified as hard rules in `docs/STYLE_GUIDE.md` — read that before starting any ticket that touches component structure, state, or API wiring. It also has a "flagged, not yet decided" section (anonymous default exports, multi-theme system, Prettier settings) to resolve as we go.

## Known open item (not a blocker)

Google Sign-In is in the UI spec's app bar, but auth isn't built on the backend yet (project order is BE → FE → DB → Auth). Ticket 2 below stubs/mocks the signed-in state instead of building real auth — revisit once backend auth lands.

---

## Phase 0 — Foundation

| #   | Ticket                                              | Status                | Depends on | Backend endpoint(s)            | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | --------------------------------------------------- | --------------------- | ---------- | ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Generate typed API client                           | Done                  | —          | N/A (uses full `openapi.json`) | Point `api:gen` at the **live running backend** (`http://localhost:3000/openapi.json`, matching Babeonym's convention) rather than a static local file — confirmed backend already serves this at `GET /openapi.json` (`src/app.ts:34`). No copy-pasting the spec file; regenerating always reflects whatever the running backend currently returns. URL will need to become environment-configurable later (dev/prod servers), not just hardcoded localhost. Base URL for the actual API client (`src/api/client.ts`) must use a `VITE_`-prefixed env var per `STYLE_GUIDE.md` §4. |
| 2   | App shell — AppBar + mocked auth + routing skeleton | Done                  | —          | N/A                            | Persistent AppBar (`src/App.tsx` currently has just one stub route). Client-selector slot is a placeholder here, wired for real in #3. Top-right: mocked signed-in state (hardcoded first name + no-op Sign Out button) — see "Known open item" above. Add top-level route structure distinguishing "no client selected" vs "client selected" layouts.                                                                                                                                                                                                                              |
| 3   | Client selection state                              | Done                  | #1, #2     | `GET /api/v1/client`           | Dropdown/selector wired into the AppBar slot from #2. Holds selected client in context/hook. Drives switch between "No Client Selected" landing page (no drawer) and "Client Selected" layout (drawer + Client Summary).                                                                                                                                                                                                                                                                                                                                                            |
| 4   | Left drawer (client nav)                            | Resolved (2026-07-11) | #5.x       | N/A                            | **Decided: no drawer.** The Client Summary cards themselves are the navigation — confirmed by building #5.9 (Pay Period card), whose rows are clickable and route directly into the pay-period-specific page. Config pages (Employees, Holidays, etc.) will follow the same pattern (click the card/row) when they're built, rather than a separate drawer.                                                                                                                                                                                                                         |

---

## Phase 1 — Client Summary

Split from one ticket into a page shell + one ticket per card (approved 2026-07-09). Data flow: the page fetches `GET /client/{clientId}/summary` **once**; cards are presentational and receive their slice as props — no per-card fetching. Cards are **display-only for now** — whether they become links into each config area (vs. a drawer, vs. both) is deliberately undecided until the page can be looked at (see #4). Each card's exact design (count vs. list, which fields) is decided per-card at build time — not pre-designed here.

| #   | Ticket                              | Status | Depends on | Backend endpoint(s)                                   | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | ----------------------------------- | ------ | ---------- | ----------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 5.1 | Client Summary page + route (shell) | Done   | #3         | `GET /api/v1/client/{clientId}/summary`               | Page component, route wiring, the single summary fetch, layout the cards slot into. Approved: summary data lives in local page state, not a context domain (see `STYLE_GUIDE.md` §3). **URL is now source of truth for client selection** (approved 2026-07-09): route is `/client/:clientId`, `selectedClient` removed from reducer, derived from the URL via `state/client/useSelectedClient.ts` — survives refresh/bookmarking (see `STYLE_GUIDE.md` §5).                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 5.2 | Client Information card             | Done   | #5.1       | N/A — uses `selectedClient`, not #5.1's summary fetch | ClientName, ClientCode. **Correction (2026-07-09): does not depend on the `ClientSummary` fetch** — `clientName`/`clientCode` live on the `Client` object (`GET /client` list, already resolved via `useSelectedClient()`), not on `ClientSummary`'s response. Renders as soon as a client is selected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 5.3 | Employees card                      | Done   | #5.1       | (data from #5.1's fetch)                              | Active count. Backend pre-filters `ClientSummary.employees` to Active-only (`getClientSummary.ts`), so there's no Inactive count to show — just `employees.length`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 5.4 | ~~Supervisors card~~                | Parked (2026-07-17) | #5.1       | (data from #5.1's fetch)                              | Removed from Client Summary after requirements review. Supervisor config currently has CRUD/API support but no current product behavior: it does not validate approver permissions, validate signature names, assign supervisors to employees, or feed reporting. Revisit with the client before rebuilding a dashboard card or management page. |
| 5.5 | Activities card                     | Done   | #5.1       | (data from #5.1's fetch)                              | List, `labels: [activityName, payrollCategory]`, via `Shared/DashboardList`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 5.6 | Funding Sources card                | Done   | #5.1       | (data from #5.1's fetch)                              | List of funding source names only (code omitted — often unset).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 5.7 | Holidays card                       | Done   | #5.1       | (data from #5.1's fetch)                              | List, `labels: [holidayName, formatUTCDateMedium(holidayDate)]`, sorted chronologically.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 5.8 | Settings card                       | Done   | #5.1       | (data from #5.1's fetch)                              | Time Input Method, Pay Period Interval, Pay Period Start Date — shown as `[label, value]` rows via `Shared/DashboardList` for visual consistency, even though this isn't a variable-length collection.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 5.9 | Pay Period card                     | Done   | #5.1       | (data from #5.1's fetch — `ClientSummary.payPeriods`) | **Supersedes the original Phase 2 Index page (see #6).** Design settled 2026-07-11: rather than a separate Pay Periods Index route, this is an 8th card on Client Summary itself — resolves #4 (cards are the navigation, no drawer). Backend now returns `payPeriods` on `ClientSummary`, pre-filtered to non-Closed (confirmed working — same pre-filtering pattern as #5.3's Employees). Row = pay period name + `Shared/PayPeriodStatusChip` (new, cross-cutting since #9 will likely reuse it) as `DashboardList`'s new `endAdornment` slot, clickable via `DashboardList`'s new `path` prop (both added to the shared component this ticket). **Open loose end:** each row's `path` currently points at a guessed route, `/client/:clientId/payPeriod/:payPeriodId` — that route doesn't exist yet (see #8, next up) so clicking a row does nothing useful until then. |

---

## Phase 2 — Create Pay Period

| #   | Ticket                     | Status                  | Depends on | Backend endpoint(s)                                                          | Notes                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | -------------------------- | ----------------------- | ---------- | ---------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 6   | ~~Pay Periods Index page~~ | Superseded (2026-07-11) | ~~#3~~     | ~~`GET /api/v1/payPeriod/{clientId}`~~                                       | **Superseded by #5.9.** No separate Index route — the pay period list lives on Client Summary as its own card instead. Keeping this row so the "we considered a standalone Index page and deliberately didn't build it" decision isn't lost.                                                                                                                                                                |
| 7   | Create Pay Period modal    | Backlog                 | #5.9       | `GET /api/v1/payPeriod/{clientId}/next`, `POST /api/v1/payPeriod/{clientId}` | **Dependency updated (2026-07-11):** was #6, now #5.9 since that's what exists. Still needs a trigger point decided at build time — likely a button on `ClientPayPeriodCard` — since there's no Index page toolbar to put it in anymore. Confirmation modal only — no user-configurable fields. Shows Name/Start/End from the `next`-suggested pay period, Create button posts it, then refreshes the list. |

---

## Phase 3 — Pay Period Layout

**Redesigned 2026-07-11** — full reasoning in `docs/PAY_PERIOD_PAGES.md`. The original single-page "Pay Period Dashboard" (with a sticky two-column footer for Payroll/Allocation actions) is replaced by a shared layout (`PayPeriodLayout`) plus three sibling pages (Phases 4-6 below), reached via `Tabs`, not the old footer. `#8.1`/`#8.2` below are historically accurate (they describe what was built and still stands) — the remaining work is the structural split.

| #    | Ticket                                    | Status  | Depends on | Backend endpoint(s)                              | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---- | ----------------------------------------- | ------- | ---------- | ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 8.1  | Page shell + route + fetch                | Done    | #5.9       | `GET /api/v1/payPeriod/{clientId}/{payPeriodId}` | Route `/client/:clientId/payPeriod/:payPeriodId`. Resolves `clientId` via `useSelectedClient` (redirect if invalid), own fetch via `useFetchByKey`. Currently lives in `PayPeriodDashboard.tsx` as one page — superseded structurally by #8.20's split, but the logic described here is unchanged and gets reused, not rebuilt.                                                                                                                                                                                 |
| 8.2  | Pay Period Info                           | Done    | #8.1       | (data from #8.1's fetch)                         | `PayPeriodInfoCard` — status chip + name, one line. Unchanged; will render from `PayPeriodLayout` once #8.20 splits the page.                                                                                                                                                                                                                                                                                                                                                                                   |
| 8.20 | Split into `PayPeriodLayout` + `Tabs` nav | Backlog | #8.1, #8.2 | N/A                                              | New work (2026-07-11). Split `PayPeriodDashboard.tsx` into `PayPeriodLayout.tsx` (renders `PayPeriodInfoCard` + `Tabs` + `Outlet`, owns the `PayPeriod` fetch + `clientId` guard) and `TimesheetStatusPage.tsx` (everything else currently in that file — see Phase 4). Add child routes `payrollReport`/`allocationReport` (can point at stub components until Phases 5/6 build them for real). Add a `refetchPayPeriod` callback via `<Outlet context={...}>`/`useOutletContext()` — see `STYLE_GUIDE.md` §5. |

---

## Phase 4 — Timesheet Status Page

Full design: `docs/PAY_PERIOD_PAGES.md` §2. This is everything currently in `PayPeriodDashboard.tsx` besides `PayPeriodInfoCard`, re-homed under `TimesheetStatusPage` once #8.20 splits it — no functional change to what's below just from the move itself, called out only where the redesign actually changed something.

| #    | Ticket                                                   | Status      | Depends on | Backend endpoint(s)                                                         | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---- | -------------------------------------------------------- | ----------- | ---------- | --------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 8.3  | Employee Timesheet Status — base list                    | Done        | #8.1       | `GET /api/v1/timesheet/status/{clientId}/{payPeriodId}`                     | `EmployeeTimesheetStatusCard` — MUI `Table`, Name/Status populated, Include/Ignore column exists (filled by #8.4). Unchanged by the redesign.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 8.4  | Employee Timesheet Status — Include/Ignore toggle        | In Progress | #8.3       | `GET`/`PUT /api/v1/payrollReport/{clientId}/{payPeriodId}/employeeExpenses` | Toggle saves immediately, reverts on failure, error shown via `Shared/ErrorTooltip`. Preemptive disable uses `EmployeeTimesheetStatus.totalHours` (already fetched, #8.3) plus `flatRateQuantity` — disable switching to Ignore when total hours is greater than 0 or flat-rate quantity is greater than 0 (treating a null `flatRateQuantity` as 0). Backend field landed 2026-07-12 (`flatRateQuantity: number` or `null` — null means the client has no flat-rate activities configured at all, not "zero this period"); API client regenerated; not yet wired into the toggle's disable logic — that's the remaining frontend work here. Not perfectly authoritative after a report's generated (live data can drift from the report's snapshot), but the existing revert-on-failure plus `ErrorTooltip` is the safety net for that. |
| 8.6  | Employee Timesheet Status — Generate Timesheets button   | Backlog     | #8.3       | `POST /api/v1/timesheet/{clientId}/{payPeriodId}/generate`                  | Unaffected by the redesign — always enabled, unrelated to Payroll/Allocation Report changes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 8.13 | Employee Timesheet Status — collapsible groups by status | Backlog     | #8.3       | (data from #8.3's fetch)                                                    | Unchanged design — see prior note. Complements #8.15, doesn't replace it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 8.14 | Employee Timesheet Status — pagination                   | Backlog     | #8.3       | (data from #8.3's fetch)                                                    | `TablePagination`, unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 8.15 | Employee Timesheet Status — column sorting               | Backlog     | #8.3       | (data from #8.3's fetch)                                                    | `TableSortLabel`, unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 8.16 | Employee Timesheet Status — Total Hours column           | Backlog     | #8.3       | (data from #8.3's fetch)                                                    | Unchanged — no new fetch needed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 8.17 | Employee Timesheet Status — link to timesheet            | Backlog     | #8.3       | (data from #8.3's fetch)                                                    | Unchanged — no new fetch needed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 8.18 | Employee Timesheet Status — refresh/reload button        | Backlog     | #8.3       | (re-runs #8.3/#8.4's fetches)                                               | Unchanged, still explicitly deferred.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 8.19 | Global toast/notification system                         | Backlog     | N/A        | N/A                                                                         | Unaffected by this redesign — general-purpose, deferred.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 8.21 | Generate Payroll Report button (relocated)               | Backlog     | #8.20      | `POST /api/v1/payrollReport/{clientId}/{payPeriodId}/generate`              | New home for what was built in the old footer (`PayPeriodDashboardFooter`/`ReportFooterSection`, now being deleted — see #8.22). Reuses the exact same `useAsyncAction` logic (already working) — just moves the JSX from the footer into this page, and the `refetchPayPeriod` call now goes through #8.20's `Outlet` context instead of a direct prop.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 8.22 | Delete `PayPeriodDashboardFooter`                        | Backlog     | #8.21      | N/A                                                                         | No more footer, anywhere — `Tabs` (#8.20) handles navigation between the three pages, and each Generate button now lives on the page whose data justifies that decision (see `docs/PAY_PERIOD_PAGES.md`). Delete `PayPeriodDashboardFooter/` and `ReportFooterSection/` once #8.21 relocates the Payroll Generate logic.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 8.23 | "Matches pay period report" indicator                    | Backlog     | #8.3       | `GET /api/v1/payrollReport/{clientId}/{payPeriodId}`                        | Only renders once `getPayrollReport` is non-null. Compares live `totalHours`/`flatRateQuantity` (#8.3's fetch) against the report's snapshot aggregates — `totalHours`/`totalFlatRate`, both landed on the backend 2026-07-12, always numbers, never null — icon plus tooltip if they disagree, "warning" severity not "error". Needs its own `GET /payrollReport/...` fetch on this page (independent from #8.4's use of `EmployeeExpense`).                                                                                                                                                                                                                                                                                                                                                                                            |
| 8.24 | Update docs — remove `timesheetFileLink` references      | Backlog     | N/A        | N/A                                                                          | Backend removed `timesheetFileLink` from `Employee` and from `GET /timesheet/status/...` (landed 2026-07-13) — it was 100% derivable from `timesheetFileId` (`https://docs.google.com/spreadsheets/d/{timesheetFileId}/edit`), no independent value. Update `PAY_PERIOD_PAGES.md` §2 (timesheet-link column) and this ticket's own #8.17 note to construct the link from `timesheetFileId` client-side instead. No functional break yet — confirmed the removed field is only referenced in the stale generated API client, not in any hand-written frontend code — but docs should be corrected before #8.17 is picked up so it isn't built against a field that no longer exists.                                                                                                                                                    |

---

## Phase 5 — Payroll Report Page

Full design: `docs/PAY_PERIOD_PAGES.md` §3. All new — the old `9.1` was a placeholder ("display format needs research"); this replaces it now that the page's actual scope is settled.

| #    | Ticket                                         | Status  | Depends on   | Backend endpoint(s)                                                                                                                                | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---- | ---------------------------------------------- | ------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 11.1 | `PayrollReportPage` — display                  | Backlog | #8.20        | `GET /api/v1/payrollReport/{clientId}/{payPeriodId}`                                                                                               | Route `payrollReport` child of `PayPeriodLayout`. Table: one row per employee, name + total hours (from the report's `totalHours` aggregate, landed 2026-07-12) — read-only part only, no Total Expense yet.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 11.2 | Total Expense field + Save All                 | Backlog | #11.1        | `PUT /api/v1/payrollReport/{clientId}/{payPeriodId}/employeeExpenses/batch`, `GET /api/v1/payrollReport/{clientId}/{payPeriodId}/employeeExpenses` | Per-employee field, joined by `employeeId` against #11.1's rows. Local/uncommitted input, disabled entirely if `activeThisPayPeriod` is false. One page-level "Save All" button. **Backend shape landed 2026-07-12**: the batch endpoint takes `EmployeeExpenseUpdate[]` (`employeeId` + `totalExpense` only) — a partial update, not a full `EmployeeExpense[]` replace. **Gotcha resolved 2026-07-13**: the endpoint originally silently ignored any `employeeId` with no existing expense record; it's now a true upsert — creates the record (name resolved from PayrollConfig, `activeThisPayPeriod: true`) when one doesn't exist yet, and rejects the whole batch with a 422 naming the id if it's not a real employee at all. No frontend-side detect/guarantee logic needed — the page can just call the batch endpoint and trust it to handle every active employeeId correctly. |
| 11.3 | Unsaved-changes guard                          | Backlog | #11.2        | N/A                                                                                                                                                | `useBlocker` protecting the unsaved batch before "Save All" — separate from "already-saved values can be corrected later," which doesn't need protecting.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 11.4 | Generate Allocation Report button (first-time) | Backlog | #11.2        | `POST /api/v1/payrollReport/{clientId}/{payPeriodId}/allocationReport`, `GET /api/v1/payrollReport/{clientId}/{payPeriodId}/allocationReport`      | Enabled once all active employees have `totalExpense` populated. Only shown/used for the **first** generation — needs a `GET allocationReport` presence check (empty array = none yet, endpoint already exists) to know whether to show this button or defer to auto-regeneration (#11.5). No parent-refetch needed — confirmed `generateAllocationReport` doesn't touch `PayPeriod.status`.                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 11.5 | Auto-regenerate Allocation Report on save      | Backlog | #11.2, #11.4 | `POST /api/v1/payrollReport/{clientId}/{payPeriodId}/allocationReport`                                                                             | Once an allocation report already exists, every subsequent "Save All" here silently triggers regeneration — no button. Failures handled silently (logged, not surfaced) — see `STYLE_GUIDE.md` §8. Same mechanism triggers from #12.2 (Additional Expenses) on the Allocation Report page.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

---

## Phase 6 — Allocation Report Page

Full design: `docs/PAY_PERIOD_PAGES.md` §4. Replaces the old `10.1`-`10.4`, which assumed a Generate button on this page — there isn't one anymore (see Phase 5).

| #    | Ticket                                | Status  | Depends on | Backend endpoint(s)                                                           | Notes                                                                                                                                                                                                                                                                                                                                                                                    |
| ---- | ------------------------------------- | ------- | ---------- | ----------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 12.1 | `AllocationReportPage` — display      | Backlog | #8.20      | `GET /api/v1/payrollReport/{clientId}/{payPeriodId}/allocationReport`         | Route `allocationReport` child of `PayPeriodLayout`. Table is **per-funding-source**, not per-employee — `AllocationReportRow`: `{ fundingSourceName, wagesAllocation, additionalExpenses, total }`. Display only, no Generate button on this page at all (see Phase 5's #11.4/#11.5).                                                                                                   |
| 12.2 | Additional Expenses + auto-regenerate | Backlog | #12.1      | `GET`/`PUT /api/v1/payrollReport/{clientId}/{payPeriodId}/additionalExpenses` | Org-level `{expenseName, amount}` entries, proportionally distributed by the backend's existing calculation. Same shape as #11.2 — typed input, explicit save (not per-field), triggers automatic allocation-report regeneration on success (same mechanism as #11.5) once a report already exists. Allocation reports aren't archived/versioned — the latest is always assumed correct. |
| 12.3 | Unsaved-changes guard                 | Backlog | #12.2      | N/A                                                                           | `useBlocker` on Additional Expense fields, same pattern as #11.3.                                                                                                                                                                                                                                                                                                                        |
| 12.4 | Close Pay Period button               | Backlog | #12.1      | `PATCH /api/v1/payPeriod/{clientId}/{payPeriodId}/close`                      | Final action in the pipeline. Enabled once an allocation report exists.                                                                                                                                                                                                                                                                                                                  |

---

## Backend work for this redesign — done (landed 2026-07-12)

Full detail in `docs/PAY_PERIOD_PAGES.md` (bottom section). API client regenerated against the updated backend (required a server restart before the new spec was actually served).

1. Done — `updateEmployeeExpenses.ts`'s `UnprocessableError` message now uses `employeeName`, not `employeeId`.
2. Done — `flatRateQuantity` (`number` or `null`) added to `EmployeeTimesheetStatus` — needed by #8.4, #8.23.
3. Done — `totalHours`/`totalFlatRate` aggregates added to `getPayrollReport`'s response, always numbers — needed by #8.23, #11.1.
4. Done, shape different from planned — new batch endpoint is a partial update (`EmployeeExpenseUpdate[]`, `employeeId` + `totalExpense` only, silently ignores unknown employee ids), not a full-array replace — see #11.2's open design question.
5. Bonus — the three kebab-case paths (`employee-expenses`, `additional-expenses`, `allocation-report`) were renamed to camelCase, resolving previously-flagged naming-convention debt. All endpoint paths in the tickets above already reflect this.

---

---

## Epic backlog — Client and payroll config management

Added 2026-07-16 after the backend CRUD/API surface expanded. These are intentionally epic-level placeholders, not implementation tickets yet. Before any epic is decomposed, discuss the workflow, screen shape, validation, navigation, and ordering. The backend routes exist so the frontend can eventually manage this configuration in-app instead of relying on direct sheet edits.

| Epic | Status | Backend endpoint(s) | Notes |
| --- | --- | --- | --- |
| Client Administration | Backlog | `GET`/`POST /api/v1/client`, `PUT /api/v1/client/{clientId}` | Build the frontend surface for creating a client and editing client identity/status. Backend client creation provisions real Drive/Sheets infrastructure, creates the Payroll Config and Pay Period Registry files, creates required config tabs, and seeds Settings. No delete flow — deactivation via `status` is the supported removal path. Needs careful UX for existing-folder links vs. create-new folder choices, since the backend deliberately does not silently create or roll back partial Drive setup. |
| Timesheet Folder Management | Done | `GET`/`POST /api/v1/timesheetFolder/{clientId}`, `PUT /api/v1/timesheetFolder/{clientId}/{timesheetFolderId}` | Completed 2026-07-17. Client Summary includes a Timesheet Folders card using `ClientSummary.timesheetFolders`, with clickable navigation to `/client/:clientId/timesheetFolders` (camelCase route segment, matching existing `payPeriod` style). Management page includes a reusable client-management shell, read-only list/table, `Show inactive` toggle, Drive folder open action, create dialog, and edit dialog for folder name/status. Drive folder ID/link is immutable after creation: create accepts a link for backend parsing, edit shows the stored Drive folder ID read-only and never allows changing it. No delete flow — inactive preserves audit/history. Employee folder choice is only a creation-time provisioning destination in Employee Management, not a durable assignment managed here. |
| Employee Management | Done | `GET`/`POST /api/v1/employee/{clientId}`, `PUT /api/v1/employee/{clientId}/{employeeId}` | Completed 2026-07-17. Client Summary Employees card navigates to `/client/:clientId/employees`. Management page uses the shared client-management shell, active-only default view with `Show inactive`, sortable employee table, create dialog, and edit dialog. Names display as `Last, First` and default-sort by last name. Pay rates are formatted via `currencyToString` and treated as calculation inputs. Create supports either `timesheetFolderId` for new workbook provisioning or `timesheetFileLink` for backend validation/extraction of an existing workbook. Persisted employee state stores `timesheetFileId`; there is no durable employee-to-folder assignment to edit/display. Edit supports identity/contact fields, pay rates, status, and read-only `timesheetFileId` with an open action. No delete endpoint — inactive status preserves audit/history. |
| Supervisor Management | Parked | `GET`/`POST /api/v1/supervisor/{clientId}`, `PUT`/`DELETE /api/v1/supervisor/{clientId}/{supervisorId}` | Parked 2026-07-17 pending client requirements discussion. Backend stores supervisors and exposes CRUD, but supervisors currently do not drive permissions, signature validation, employee assignment, generated timesheet behavior, or reporting. Dashboard card was removed to avoid implying the config has active product meaning. Leave backend/API/model support in place for now; decide later whether supervisors should gain a real workflow purpose or be removed. |
| Funding Source Management | Backlog | `GET`/`POST /api/v1/fundingSource/{clientId}`, `PUT`/`DELETE /api/v1/fundingSource/{clientId}/{fundingSourceId}` | Build the funding source list/create/edit/delete surface. Fields include `fundingSourceName` and optional `fundingSourceCode` for QuickBooks mapping. This should probably be designed before Activity Management, because activities allocate percentages across funding source names. |
| Activity Management | Backlog | `GET`/`POST /api/v1/activity/{clientId}`, `PUT`/`DELETE /api/v1/activity/{clientId}/{activityId}` | Build the activity list/create/edit/delete surface. This is likely the heaviest config epic: activities include `activityName`, `trackSeparately`, `payrollCategory`, up to 3 funding-source percentage allocations, `payRate`, and `flatRateAmount`. Backend applies/validates funding source allocation rules; frontend should make those rules visible and hard to misuse. Activity pay-rate choices feed payroll/allocation calculations directly. |
| Holiday Management | Ready | `GET`/`POST /api/v1/holiday/{clientId}`, `PUT`/`DELETE /api/v1/holiday/{clientId}/{holidayId}` | Build the holiday list/create/edit/delete surface. Holidays are client-wide dates used in generated timesheets and payroll calculations: they mark holiday columns, feed holiday-hours aggregation, and affect holiday pay handling. Model shape is `holidayId`, `holidayName`, `holidayDate`. Hard delete exists, so delete must be confirmed. Decomposed below. |
| Settings Management | Parked | `GET`/`PUT /api/v1/settings/{clientId}` | Do not decompose until "Resolve settings edit logic" is settled below. Settings are already seeded during Client creation. Editing may be valid but uncommon, and `payPeriodStartDate` should likely be locked once any pay period exists. Need a backend + frontend business-rule pass before treating this as ordinary CRUD. |

---

## Decomposed epic — Employee Management

Added 2026-07-17 after initial requirements/design discussion. Use Timesheet Folder Management as the style and implementation reference: same Client Summary card navigation pattern, `ClientManagementPage` shell, management toolbar/list/table/dialog conventions, inactive filtering, refetch-after-save flow, async button behavior, and error-message handling. While building each slice, look for shared component opportunities before adding employee-specific code; use existing shared management primitives where they fit, and create new shared primitives only when the employee and timesheet-folder pages have a real common need.

Important backend shape: `POST /api/v1/employee/{clientId}` accepts exactly one of `timesheetFileLink` or `timesheetFolderId`. Supplying `timesheetFileLink` lets the backend validate the pasted Google Sheets/Drive URL, extract the file id, and persist it as `Employee.timesheetFileId`. Supplying `timesheetFolderId` provisions a new workbook in an Active Timesheet Folder, but the persisted `Employee` model still only stores `timesheetFileId`. Do not design the frontend as though a durable employee-to-folder field exists unless the backend/API changes. Pay Period behavior stays out of this epic except for noting that pay-period generation consumes employee `timesheetFileId` later.

| # | Ticket | Status | Depends on | Backend endpoint(s) | Notes |
| --- | --- | --- | --- | --- | --- |
| EM.1 | Employee Management route + read-only list | Done | Timesheet Folder Management | `GET /api/v1/employee/{clientId}` | Linked the Client Summary Employees card to `/client/:clientId/employees`. Added the management page using the existing client-management shell and fetching all employees for the selected client. Defaults to active employees only, with a `Show inactive` toggle matching Timesheet Folder Management. Table columns: Name, Position, Email, Status, Pay Rates, Timesheet. Name display is `Last, First`, default-sorted by last name. Table headers are sortable where there is meaningful row data to sort. Pay rates are shown as calculation inputs and formatted through `currencyToString`. Timesheet action opens `https://docs.google.com/spreadsheets/d/{timesheetFileId}/edit` when present. Empty states: `No active employees.` by default, `No employees.` when showing inactive. Also wired the generated `EmployeeApi` through `src/api/client.ts`. |
| EM.2 | Create employee dialog | Done | EM.1 | `POST /api/v1/employee/{clientId}`, `GET /api/v1/timesheetFolder/{clientId}` | Added create workflow using the existing management-dialog style. Fields: first name, last name, position, email, active/inactive status, hourly pay rate 1, hourly pay rate 2, holiday pay rate. Status uses shared `StatusSwitch`. Timesheet provisioning supports exactly one create-time choice: `Create New` shows a `Destination folder` selector populated from Active Timesheet Folders and sends `timesheetFolderId`; `Use Existing` shows a `Timesheet URL` input and sends `timesheetFileLink` for backend validation/extraction. Refetches employees after create. |
| EM.3 | Edit employee dialog | Done | EM.2 | `PUT /api/v1/employee/{clientId}/{employeeId}` | Added edit workflow for identity/contact fields, pay rates, status, and read-only `timesheetFileId`. Since the API does not persist `timesheetFolderId`, there is no editable or displayed folder assignment. Updating the timesheet file/workbook is intentionally out of scope for now because it carries historical Google Sheets migration concerns. Refetches employees after save. |
| EM.4 | Shared status and currency cleanup | Done | EM.2, EM.3 | N/A | Added shared `StatusSwitch` for Active/Inactive entity status editing and reused it in Employee Management and Timesheet Folder edit. Consolidated currency display/input normalization into `currencyToString`: plain `x.xx` for form normalization and decorated `$x.xx` for display. |
| EM.5 | Durable folder assignment | Not planned | N/A | N/A | Explicit non-feature for this epic. Employee creation can use a Timesheet Folder as a provisioning destination, but the employee does not track folder assignment after creation. Persisted employee timesheet linkage is `timesheetFileId`; Pay Period should consume that. |

---

## Decomposed epic — Holiday Management

Added 2026-07-17 after Supervisor Management was parked pending client requirements. Use Employee Management and Timesheet Folder Management as the style and implementation references: Client Summary card navigation, `ClientManagementPage`, `ManagementListPanel`, `ManagementToolbar`, `ManagementTable`, `ManagementDialog`, refetch-after-save/delete, async action handling, and `resolveErrorMessage`.

Backend shape: `Holiday` has `holidayId`, `holidayName`, and `holidayDate`. The generated frontend model converts `holidayDate` to a `Date` and serializes it back to `YYYY-MM-DD`. Backend validates ISO date strings on create/update. Holidays are shared across all employees for a client, have no active/inactive status, and can be hard-deleted. Holiday configuration affects generated timesheet holiday rows and payroll holiday-hour handling, so delete/edit UX should make the calculation impact clear without adding pay-period behavior to this epic.

| # | Ticket | Status | Depends on | Backend endpoint(s) | Notes |
| --- | --- | --- | --- | --- | --- |
| HM.1 | Holiday Management route + read-only list | In Progress | Employee Management | `GET /api/v1/holiday/{clientId}` | First slice. Export `holidayApi` from `src/api/client.ts`. Link the Client Summary Holidays card to `/client/:clientId/holidays`. Add route/page using the shared client-management shell and fetch all holidays for the selected client. Table columns: Holiday and Date. Default-sort by `holidayDate` ascending; table headers should be sortable for name/date. Date display should reuse existing date formatting (`formatUTCDateMedium`) unless implementation discovers a stronger existing pattern. No create/edit/delete controls yet unless explicitly approved for the slice. Empty state: `No holidays.` |
| HM.2 | Create/edit holiday dialog | Backlog | HM.1 | `POST /api/v1/holiday/{clientId}`, `PUT /api/v1/holiday/{clientId}/{holidayId}` | Add create/edit workflow using shared management-dialog conventions. Fields: holiday name and holiday date. Both are required. Date input should send a date value that the generated API serializes as `YYYY-MM-DD`; avoid timezone drift when loading/displaying existing holidays. Refetch holidays after create/edit. |
| HM.3 | Delete holiday confirmation | Backlog | HM.1 | `DELETE /api/v1/holiday/{clientId}/{holidayId}` | Add delete action with confirmation. Copy should state that deleting removes the holiday from client configuration and can affect future generated timesheets/payroll calculations. Refetch holidays after delete. |
| HM.4 | Docs + epic completion | Backlog | HM.1, HM.2, HM.3 | N/A | After user review/approval, mark Holiday Management complete in the epic backlog and document final behavior. |

---

## Future / not yet scheduled

| Ticket                     | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Resolve settings edit logic | Parked (2026-07-16): decide whether `PUT /settings/{clientId}` should remain fully open, become constrained, or be removed from normal frontend workflows. Current concern: settings are created with Client creation; `payPeriodStartDate` only matters before the first pay period and should likely be locked after any pay period exists. `payPeriodInterval` and `timeInputMethod` may be valid future-only edits, but those rules need explicit requirements and backend enforcement before a Settings Management screen is designed. |
| Resolve supervisor value | Parked (2026-07-17): talk through Supervisor requirements with the client before building Supervisor Management. Current Supervisor config is not used for permissions, signature-name validation, employee assignment, timesheet generation, or reporting. Possible future purposes: validate signed supervisor names, provide a controlled approval/signature workflow, include approving supervisor in audit/report output, email/share timesheets with supervisor contacts, or remove the object if none of those are useful. |
| Write component/unit tests | Deferred (2026-07-09): testing philosophy and the tested-vs-not criteria are settled and documented in `STYLE_GUIDE.md` §11 (test what has branching/computation/is shared/is async; skip pure passthrough). Deliberately not writing tests yet — Client Summary's cards are still actively changing shape (headers, `DashboardCard` migration, list-vs-count decisions all changed today alone), and the nav-vs-drawer decision (#4) could still change how cards behave. Testing against a moving target risks rewriting tests repeatedly. Return to this once Client Summary settles down — likely candidates when we do: `client.reducer.ts` (cheap, stable, pure), `useSelectedClient`, `ClientSummary.tsx`'s branches, `DashboardCard`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Per-client accent color    | Idea (2026-07-09): give each client a distinct color, applied as the accent/primary color once that client is selected (app bar, possibly card accents) — an instant visual cue for which client you're on, catches "wrong client" mistakes. **Approach not yet decided**, two options on the table: (1) stored field on `Client` (`clientColor` added to the `Clients` sheet/schema, backend + FE change, curatable but needs manual data entry per client and someone remembering to set it) — user has confirmed the schema change itself is low-effort if this path is chosen; (2) derived deterministically from `clientId` (hash → HSL color, constrained saturation/lightness band for legible contrast, zero backend change, automatic for every client including existing ones, but not hand-curatable). Leaning #2 in discussion but not settled — user wants to understand the hashing approach better before deciding. No urgency — option 2 has no schema dependency so there's no cost to deciding late; only option 1 would benefit from earlier commitment. Low-cost prep in the meantime: style new components (e.g. `DashboardCard`) using `theme.palette.primary.main` rather than hardcoded colors, so either approach slots in later with no rework. |

---

## Decisions already made (so we don't re-litigate them)

- **State management**: no new library. Plain React state/hooks (`useState`/`useContext`) — the app is button-triggered throughout, not live/reactive, so there's no need for TanStack Query/Redux/etc.
- **Logging**: skipped. This is a small internal tool for a small company, not a public-facing product — no automated error-tracking/observability layer planned.
- **Auth**: mocked/stubbed only (see "Known open item" above) until backend auth work begins.
