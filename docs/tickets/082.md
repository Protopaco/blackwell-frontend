# [082] Split EmployeeExpenses `totalExpense` into `wageExpense` and `taxExpense`

**Board status:** Backlog
**Reported:** 2026-08-12

## Background

This spans both `blackwell-backend` and `blackwell-frontend`.

Per-employee expenses on the Payroll Report currently track a single `totalExpense` number. We need to
separate it into two fields, `wageExpense` and `taxExpense`, per employee per pay period. Not yet clear
how the two will be consumed downstream — in particular, `buildAllocationRows.ts` currently uses
`totalExpense` directly as the wage-allocation numerator (proportioned across funding sources by
hours-weighted cost, summed into `processedWagesTotal`). Whether that becomes `wageExpense` alone, the
sum of both, or something else needs to be resolved as part of this ticket — don't guess, confirm with
the client/business side before changing the allocation formula.

## Full touchpoint inventory (`totalExpense`)

**Backend**
- `src/models/EmployeeExpense.ts` — field definition
- `src/models/EmployeeExpenseUpdate.ts` — field definition (batch PUT payload)
- `src/models/EmployeePayrollSummary.ts` — field in payroll report response shape
- `src/config/constants.ts` — `EMPLOYEE_EXPENSES_HEADERS`, spreadsheet column header for the
  `EmployeeExpenses` tab (becomes two columns)
- `src/utils/swagger/schema/employeeExpense.ts`, `employeeExpenseUpdate.ts` — OpenAPI schema defs
- `src/db/payrollReport/readEmployeeExpensesTab.ts` — reads/parses the cell from the sheet row
- `src/db/payrollReport/writeEmployeeExpensesTab.ts` — writes the value back to the sheet
- `src/services/payrollReport/updateEmployeeExpenses.ts` — single-employee PUT passthrough
- `src/services/payrollReport/updateEmployeeExpensesBatch.ts` — builds update map, applies to new records
- `src/services/payrollReport/mergeEmployeeExpenseTotals.ts` — overlays incoming updates onto existing
  records by `employeeId`
- `src/services/payrollReport/buildPayrollReportResponse.ts` — joins the field into the payroll report
  response by `employeeId`
- `src/services/payrollReport/buildAllocationRows.ts` — **the open question above**; also filters out
  employees where the field is `null`
- `src/routes/v1/payrollReport/putEmployeeExpensesBatch.ts` — JSDoc references, update wording
- `src/devTestData/scenarios/createClosedPayPeriod.ts` — seed data
- Test coverage to update: `tests/unit/payrollReport/mergeEmployeeExpenseTotals.test.ts`,
  `updateEmployeeExpensesBatch.test.ts`, `buildPayrollReportResponse.test.ts`,
  `buildAllocationRows.test.ts`, `tests/unit/db/writeEmployeeExpensesTab.test.ts`,
  `tests/integration/payrollReport/{getAllocationReport,putEmployeeExpensesBatch,postGenerateAllocationReport,putEmployeeExpenses}.test.ts`,
  `tests/integration/payPeriod/patchClosePayPeriod.test.ts`

**Frontend**
- `src/api/generated/**` — regenerate from updated OpenAPI spec (`EmployeeExpense.ts`,
  `EmployeeExpenseUpdate.ts`, `V1GetPayrollReport200ResponseValue.ts`, `PayrollReportApi.ts` JSDoc)
- `src/components/PayPeriod/PayrollReportTable/PayrollReportRow.ts` — row model field
- `src/components/PayPeriod/PayrollReportTable/PayrollReportTable.tsx` — column header (`'Total Expense'`
  becomes two headers)
- `src/components/PayPeriod/PayrollReportTable/PayrollReportTableRow/PayrollReportTableRow.tsx` —
  currency input(s), aria-labels
- `src/pages/PayPeriod/PayrollReportPage/PayrollReportPage.tsx` — maps API response into row state,
  dirty/changed diff, `allExpensesComplete` gating (currently gated on non-null `totalExpense` — decide
  if both new fields must be non-null, or just one), builds `EmployeeExpenseUpdate[]` save payload

## Scope

- Rename/split `totalExpense` → `wageExpense` + `taxExpense` across every touchpoint above (backend
  model/schema/db/service/route layer, then frontend generated client + Payroll Report table/page).
- Update the `EmployeeExpenses` sheet tab to two columns instead of one.
- Resolve the open question on `buildAllocationRows.ts`'s wage-allocation numerator before touching that
  file's logic.
- Update all listed backend tests; add frontend coverage if any exists for the Payroll Report expense
  inputs.
