# [004] Unsaved-changes guard — Allocation Report

**Board status:** Backlog
**Migrated from:** old `docs/TICKETS.md` #12.3

## Background

Same concern as [003], on `AllocationReportPage`'s Additional Expenses editor (`expenseRows`) — local edits are uncommitted until "Save Additional Expenses" is clicked, with nothing currently blocking navigation away from an unsaved row.

## Scope

`useBlocker` guard on the Additional Expenses fields, same pattern as [003]. Likely built together with [003] given the shared mechanism, but tracked as a separate ticket since they're two different pages/forms.

## Open questions

- Same dialog-component question as [003] — resolve once, apply to both.
