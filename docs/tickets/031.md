# [031] Employees tab — rename + remove action

**Board status:** Backlog
**Part of epic:** [030] Per-Pay-Period Configuration UI
**Depends on:** [027] Edit Employees within a pay period (backend)

## Background

The existing Timesheet Status tab (`TimesheetStatusPage.tsx`) already displays the pay period's Employee snapshot data (redirected to the snapshot per [025]) — no new page, fetch, or component is needed for display. This ticket is scoped to the rename and the one missing action.

## Scope

- Rename the tab label from "Timesheet Status" to "Employees" in `PayPeriodLayout.tsx`'s `Tabs`/`Tab` block (route path/URL can stay as-is unless that's confirmed to need changing too).
- Add a remove action per employee row, calling [027]'s remove-employee endpoint, enabled only when that employee has no generated timesheet yet for this period.
- When remove is unavailable, indicate why rather than just omitting the control (matches the existing precedent from [020] — explain, don't silently hide).
- Add already works today via the existing "generate a timesheet for an employee added after others" mechanic — confirm whether an explicit "Add employee" action is still needed here or if that flow already covers it.
