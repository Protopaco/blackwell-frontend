# [008] Settings Management

**Board status:** Parked
**Migrated from:** old `docs/TICKETS.md` "Settings Management" epic + "Resolve settings edit logic" note

## Background

Settings (`GET`/`PUT /api/v1/settings/{clientId}`) are already seeded during Client creation. No management UI exists yet — parked 2026-07-16 pending a business-rule decision, not a UI design question.

## Open question (blocking — needs a backend + frontend business-rule pass before this can be scoped as ordinary CRUD)

- `payPeriodStartDate` only matters before the first pay period exists — should it become locked/uneditable once any pay period has been created for the client?
- `payPeriodInterval` and `timeInputMethod` may be valid future-only edits (i.e. changeable going forward without affecting past pay periods), but the exact rules for what's safe to edit and when aren't defined yet.

## Scope

Not scoped — do not decompose into a Settings Management screen until the question above is settled.
