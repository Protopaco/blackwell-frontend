# [009] Employee Timesheet Status — collapsible groups by status

**Board status:** Parked
**Migrated from:** old `docs/TICKETS.md` #8.13

## Background

Superseded for now by #8.25 (name search + Complete/Incomplete filter + mismatch sort) and #8.14 (pagination), both already built and covering the same "long list is hard to scan" ergonomics need a different way.

## Scope

Parked, not built. Revisit only if the client specifically asks for status-based grouping after having seen the filtered/paginated table in practice — don't build preemptively.
