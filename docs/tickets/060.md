# [060] "+ New Client" entry point

**Board status:** Backlog
**Part of:** [059] Client Management Workflow (Epic)

## Background

No entry point exists yet to create a client — everything in the app today is scoped under an already-selected `:clientId`. See [059] for the full design.

## Scope

- Add a "+ New Client" button to `Header.tsx`, next to `ClientSelector`
- Add a `/client/new` route in `router.tsx`, outside the `:clientId`-scoped route tree, rendering a placeholder page (the real form is [061])
