# [007] Supervisor Management Epic

**Board status:** Parked
**Migrated from:** old `docs/TICKETS.md` "Supervisor Management" epic + "Resolve supervisor value" note

## Background

Parked 2026-07-17 pending a client requirements conversation. Backend already stores supervisors and exposes CRUD, but supervisors currently do not drive permissions, signature validation, employee assignment, generated-timesheet behavior, or reporting — no real product meaning yet. The Client Summary Supervisors card was removed specifically to avoid implying the config has active behavior it doesn't.

## Open question (blocking — needs client conversation before any build work)

What, if anything, should Supervisor config actually do? Possible future purposes raised so far:
- Validate signed supervisor names
- Provide a controlled approval/signature workflow
- Include the approving supervisor in audit/report output
- Email/share timesheets with supervisor contacts
- Or: conclude none of these are useful and remove the object entirely

## Scope

Not scoped — deliberately not decomposed into build tasks until the question above is resolved. Backend/API/model support stays in place in the meantime.
