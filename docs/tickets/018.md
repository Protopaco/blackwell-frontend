# [018] Adjust Colors of Timesheet Status Chips

**Board status:** Backlog
**Migrated from:** old `docs/TICKETS.md` "Timesheet status chips — spacing" note (noted 2026-07-21)

## Background

`EmployeeTimesheetStatusChip` (`src/components/Shared/EmployeeTimesheetStatusChip/EmployeeTimesheetStatusChip.tsx`) — the Generated and Approved status chips need more visual separation/spacing. Not designed — purely a visual polish pass.

## Related idea, not in this ticket's scope unless folded in

Once [010] (Client Accent Color) lands, there's a related idea to derive these chips' colors from the client's accent color instead of the current 5 unrelated hardcoded MUI palette colors (`default`/`info`/`warning`/`primary`/`success`) — convert the client's base color to HSL and step lightness/hue evenly across the fixed 5-status progression for an ombre effect tied to client identity. That's a color-scheme change, distinct from this ticket's spacing/separation concern — worth deciding whether to combine or keep separate when [010] is picked up.
